services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: larakit_app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - larakit

  nginx:
    image: nginx:alpine
    container_name: larakit_nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - larakit

  mysql:
    image: mysql:8.0
    container_name: larakit_mysql
    restart: unless-stopped
    ports:
      - "3307:3306"
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - larakit
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: larakit_redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    networks:
      - larakit

  horizon:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: larakit_horizon
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    command: php artisan horizon
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - larakit

volumes:
  mysql_data:
  redis_data:

networks:
  larakit:
    driver: bridge